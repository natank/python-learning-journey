# Database Configuration
POSTGRES_USER=cafe_user
POSTGRES_PASSWORD=cafe_password
POSTGRES_DB=cafe_db

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=1

# Database URL (auto-constructed in compose.yml, but can override)
# DATABASE_URL=postgresql://cafe_user:cafe_password@localhost:5432/cafe_db
